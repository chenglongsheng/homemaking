<view class="container">
    <view class="item" wx:key="index" wx:for="{{count}}">
        <i-icon name="{{icon}}" size="{{size}}"
                color="{{index < selected ? selectedColor : defaultColor}}">
        </i-icon>
    </view>
</view>